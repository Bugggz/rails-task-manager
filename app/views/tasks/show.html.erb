<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "Go back to the task list", tasks_path %>
<br>
<%= link_to "Edit the task", edit_task_path(@task) %>
<br>
<%= link_to "Delete the task", task_path(@task), method: :delete %>
