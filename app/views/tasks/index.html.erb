<h1>... and while you're at it...</h1>
<% @tasks.each do |task| %>
  <h3><%= link_to task.title, task_path(task) %> <%= task.completed %></h3>
  <%#= link_to "Delete the task", delete_task_path(task), method: :delete %>
<% end %>

